{
  "name": "bend-tetris",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "./scripts/bend index.html -o dist",
    "serve": "node server.mjs",
    "test:proof": "./scripts/bend PROOF.bend",
    "test:unit": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test tests/smoke.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
