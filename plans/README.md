# 改善計画

2026-09-24 に `improve` スキルで作成。基準コミットは `55d4db0`。各計画を単独で読み、STOP 条件と検証手順に従う。実行者は完了時に状態行を更新する。

## 実行順と状態

| 計画 | 内容 | 優先度 | 工数 | 依存 | 状態 |
|---|---|---|---|---|---|
| [001](001-confine-static-files.md) | 静的配信を dist 内に限定 | P1 | S | なし | DONE |
| [002](002-limit-jev-api-use.md) | Jev API 利用範囲を制限 | P1 | M | 001 | TODO |
| [003](003-timeout-jev-request.md) | Jev 応答停止時に代替へ進む | P2 | S | 002 | TODO |

状態は TODO / IN PROGRESS / DONE / BLOCKED / REJECTED を使う。

## 依存関係

- 001 でサーバー境界のテスト基盤を作り、002 と 003 で再利用する。
- 002 と 003 は同じ上流 `fetch` に触るため、この順序にする。
- サーバー境界テストの不足は独立した計画にせず、各計画の完了条件に含めた。

## 検討して採用しなかった候補

- Jev の即時配置とプレイヤーの重力進行の差: 競争ルールの意図が文書から確定できないため、バグとして計画しない。
- `CONTEXT.md` に詳細設計を追記すること: 現時点では具体的な誤動作や手戻りの根拠がない。

## 監査範囲

主要な JavaScript、サーバー、既存テスト、README、ドメイン文書を確認し、`npm run test:unit` は14件成功。Bend 証明の数学的妥当性、実際の TypeSafe AI 応答、ブラウザ E2E は今回検証していない。
